import { StyleSheet, Text, View } from 'react-native'
import * as Battery from 'expo-battery'
import React, { useEffect, useState } from 'react'

export default function BatteryApp() {
    const [isAvailable,setIsAvailable]= useState(false)
    const [batteryInfo,setBatteryInfo]=useState(undefined)

    useEffect(()=>{
        async function checkAvalability(){
            const isBatteryAvbl= await Battery.isAvailableAsync()
            setIsAvailable(isBatteryAvbl)
        }
        checkAvalability()
    })

    const shoBatteryInfo=()=>{
        console.log(batteryInfo);
        
    }
  return (
    <View>
      <Text>BatteryApp</Text>
       
      <Text>Low Power Mode: {batteryInfo.batteryLevel *100}%</Text>
    </View>
  )
}

const styles = StyleSheet.create({})
